# Swap (local emulator)

Instant swap ticket. Fixture quotes are `stale`. `SubmitSwap` rejects stale quotes. Preview/result are improvised.

Gates: session, KYC `approved`, step-up, `requestId`. No live price feed.
