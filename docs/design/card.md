# Card (local emulator)

Frozen card is a first-class state. Active card / transactions screens do not exist.

## Balances

EURx may be negative. Domain uses `Money`. Restore does not invent a rail — it routes to `swap`.

## Gates

Session required. Unfreeze only when the FIATx balance is eligible (not negative). No PAN, no invented processor.
