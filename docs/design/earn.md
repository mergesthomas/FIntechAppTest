# Earn (local emulator)

Savings hub only. Product subscribe / vault / recurring flows are not implemented.

## Settlement

`SetEarnInNexo` and `StopEarning` carry `requestId` + step-up. Statuses: `inFlight` | `unknown` | `confirmed` | `failed`.

## Gates

Session + KYC `approved` on preference change and stop. Do not invent APY beyond screenshot placeholders.

Do not implement SubscribeFixedTerm / OpenWealthVault / CreateRecurringBuy / OpenDualInvestment.
