# Explore (local emulator)

Discovery feed from screenshot fixtures. Prices are `stale`. APY and promo copy are placeholders for compliance review.

No trade submit. Perpetual rows open `futures`. Asset rows do not open wallet (blocked). Recurring Buy / Fixed Term stay closed.
