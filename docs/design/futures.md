# Futures (local emulator)

Ticket + open position + improvised preview/close. Fixture bid/ask/mark are `stale`.

`SubmitFuturesOrder` and mark-dependent `SetTakeProfitStopLoss` reject stale quotes. Size and P/L are `Money`. Session + KYC + step-up + `requestId` on submits.
